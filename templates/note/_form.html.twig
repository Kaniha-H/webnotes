{{ form_start(form) }}
    <div class="form-group">
        {{ form_label(form.content) }}
        {{ form_widget(form.content) }}
        {{ form_errors(form.content) }}
        {{ form_help(form.content) }}
    </div>
{{ form_end(form) }}
    {# <button type="submit">{{ label | default("Créer la note") }}</button> #}